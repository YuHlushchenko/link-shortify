{
  "version": 2,
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "yarn install && cd client && yarn build",
  "outputDirectory": "client/.next"
}
