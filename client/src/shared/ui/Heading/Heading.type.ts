/* eslint-disable no-unused-vars */
import { ThemeColor } from '@/shared/const/theme'

//! only those colors which can be used as Heading color
export enum HeadingColor {
  TEXT = ThemeColor.TEXT_COLOR,
  TEXT_INVERTED = ThemeColor.TEXT_COLOR_INVERTED,
  ACCENT = ThemeColor.ACCENT_COLOR,
  PRIMARY = ThemeColor.PRIMARY_COLOR_1,
  SECONDARY = ThemeColor.SECONDARY_COLOR_1,
}
