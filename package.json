{
  "name": "link-shortify",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/YuHlushchenko/link-shortify.git",
  "author": "YuHlushchenko <yuhlushchenko@gmail.com>",
  "license": "MIT",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "engines": {
    "node": ">=18.0.0",
    "npm": "please-use-yarn",
    "yarn": ">=4.0.0"
  },
  "workspaces": [
    "client",
    "server",
    "infrastructure/cdk",
    "infrastructure/sst"
  ],
  "devDependencies": {
    "esbuild": "^0.25.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2"
  },
  "scripts": {
    "prepare": "if [ \"$HUSKY\" != \"0\" ]; then husky; fi",
    "esbuild": "esbuild",
    "docs:api": "bash docs/swagger/serve.sh",
    "deploy:server:dev": "STAGE=dev yarn workspace link-shortify-infra cdk deploy --all --require-approval never --asset-parallelism=false --profile shortify",
    "deploy:server:prod": "STAGE=prod yarn workspace link-shortify-infra cdk deploy --all --require-approval never --asset-parallelism=false --profile shortify",
    "deploy:client:dev": "AWS_PROFILE=shortify yarn workspace link-shortify-sst sst deploy --stage dev",
    "deploy:client:prod": "AWS_PROFILE=shortify yarn workspace link-shortify-sst sst deploy --stage prod"
  }
}
