name: Server CI

on:
  push:
    paths:
      - "server/**" # Виконуємо дії тільки при змінах в папці backend
  pull_request:
    paths:
      - "server/**"

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:latest
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongo --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          cd server
          yarn install

      - name: Build TypeScript
        run: |
          cd server
          yarn build

      - name: Start app
        env:
          MONGODB_URI: mongodb://localhost:27017/link-shortify
        run: |
          cd server
          yarn start
